buildscript {
    repositories {
        maven {
            mavenCentral()
            url "https://plugins.gradle.org/m2/"
        }
    }
}

apply from: "${project.rootDir}/gradle/config/core.gradle"
